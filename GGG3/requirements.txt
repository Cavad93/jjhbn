# === Core runtime ===
numpy>=1.24.0
pandas>=2.0.0
scipy
scikit-learn>=1.3.0
requests
urllib3
python-dotenv
web3
river>=0.20.0
matplotlib
cma>=3.3.0

# === ML models ===
xgboost>=2.0.0
lightgbm

# === NN head (optional; if using nn/nn_auc_head.py) ===
torch>=2.0.0
libauc

# === Exchange APIs (for paper trading and real trading) ===
python-binance>=1.0.19
ccxt>=3.1.1,<4.0.0  # v3.x doesn't require coincurve (Windows compatible)

# === Technical Analysis ===
# TA-Lib removed - using custom indicators in indicators/technical.py
# All indicators (RSI, MACD, Bollinger Bands, etc.) are implemented with pure pandas/numpy

# === Dashboard and Visualization ===
plotly>=5.17.0
dash>=2.14.0

# === Database (PostgreSQL for Smart Collector) ===
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
tqdm>=4.65.0  # Progress bars (including async support)
APScheduler>=3.10.0  # Task scheduling

# === Telegram Bot (for notifications) ===
python-telegram-bot>=20.0

# === Testing ===
pytest

# === Compatibility (only for older Python versions) ===
backports.zoneinfo; python_version < "3.9"
